[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  external_node_modules = ["pdf-lib", "nodemailer"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
